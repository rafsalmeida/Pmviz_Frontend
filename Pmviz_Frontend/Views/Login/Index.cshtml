@model Pmviz_Frontend.Models.User

@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_EmptyLayout.cshtml";
}
@addTagHelper *, Kendo.Mvc


<!--<div class="container">
    <div class="k-card">

        <div class="k-card-header">
            <h4 class="k-card-title" id="title">Login</h4>
        </div>
        <br />
        <div class="k-class-body" id="tickets">
            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewBag.Error
                </div>
            }
            <form method="post" id="ticketsForm" kendo-validator="true">
                <ul id="fieldlist">
                    <li class="divfield">
                        <label for="username" class="required">Username</label>
                        @(Html.Kendo().TextBox()
                                  .Name("username")
                                  .HtmlAttributes(new { placeholder = "Username", required = "required", @class = "inputfield" })
                        )

                    </li>
                    <li>

                        <label for="password" class="required">Password</label>
                        @(Html.Kendo().TextBox()
                                  .Name("password")
                                  .HtmlAttributes(new { placeholder = "Password", required = "required", type = "password", @class = "inputfield" })
                        )
                    </li>
                    <li>
                        <button class="k-button k-primary" type="submit">Login</button>
                    </li>
                    <li>
                        <a href="/register">Don't have an account? Register.</a>
                    </li>
                </ul>
            </form>
        </div>
    </div>
</div>-->
<div class="account-pages mt-5 mb-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6 col-xl-5">
                <div class="card bg-pattern">
                    <div class="card-body p-4">
                        <div class="text-center w-75 m-auto">
                            <div class="auth-logo">
                                <h4>PMVIZ</h4>
                            </div>
                            <p class="text-muted mb-4 mt-3">Insere o teu username e password para iniciar sessão.</p>
                            @if (ViewBag.Error != null)
                            {
                                <div class="alert alert-danger" role="alert">
                                    @ViewBag.Error
                                </div>
                            }
                        </div>
                        <form method="post">
                            <div class="form-group mb-3">
                                <label for="username">Username</label>
                                <input class="form-control" type="text" name="username" required="" placeholder="Insere o username">
                            </div>
                            <div class="form-group mb-3">
                                <label for="password">Password</label>
                                <div class="input-group input-group-merge">
                                    <input type="password" name="password" class="form-control" placeholder="Insere a password">
                                    <div class="input-group-append" data-password="false">
                                        <div class="input-group-text">
                                            <span class="password-eye"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="form-group mb-0 text-center">
                                <button class="btn btn-primary btn-block" type="submit"> Log In </button>

                            </div>
                            <div class="col-12 text-center">
                                <p>Não tens uma conta? <a href="/register"><b>Cria uma conta.</b></a></p>
                            </div> <!-- end col -->
                        </form>
                    </div> <!-- end card-body -->
                </div>
                <!-- end card -->
                <!-- end row -->
            </div> <!-- end col -->
        </div>
        <!-- end row -->
    </div>
    <!-- end container -->
</div>

<!-- end page -->
<!-- Vendor js -->

<script src="../assets/js/vendor.min.js"></script>



<!-- App js -->

<script src="../assets/js/app.min.js"></script>


<!--<style>
    #fieldlist {
        margin: 0;
        padding: 0;
    }

        #fieldlist li {
            list-style: none;
            padding-bottom: .7em;
            text-align: left;
            display: flex;
            flex-wrap: wrap;
            text-align: center;
            justify-content: center;
        }

        #fieldlist label {
            width: 100%;
            display: block;
            padding-bottom: .3em;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 12px;
            color: #444;
        }

    #title {
        width: 100%;
        display: block;
        padding-bottom: .3em;
        font-weight: bold;
        text-transform: uppercase;
        font-size: 30px;
        color: #444;
    }

    .confirm {
        padding-top: 1em;
    }

    .container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
    }

    .k-card {
        width: 300px;
        margin: 5%;
    }

    .k-card-header {
        text-align: center;
    }

    .inputfield {
        width: 300px;
    }

    .alert-danger {
        text-align: center;
    }
</style>-->




